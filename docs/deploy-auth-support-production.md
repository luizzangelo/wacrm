# Deploy controlado Auth sem SMTP — preparação, bloqueado antes de mutation

18/09/2026. Autorização recebida para deploy em produção única WACRM. **Deploy NÃO realizado. Migration NÃO aplicada.** O gate de build foi identificado antes de alterar o banco: não há Docker neste Mac nem worker externo configurado/confirmado. O Docker acessível está na VPS compartilhada de produção, com apenas ~940–950 MiB de RAM disponível e ~811–812 MiB de swap em uso. O runbook de Auth e a documentação in-place exigem aprovação/orçamento de recursos para build pesado nessa VPS; não foi assumida essa autorização específica nem arriscados os outros serviços.

Não houve falha de migration/build/deploy/healthcheck: nenhuma dessas mutations foi tentada. A pausa é preventiva por falta de caminho de build com risco controlado. Não aplicar a migration enquanto esse caminho não estiver definido. O backup concluído terá de ser reavaliado/renovado se houver deltas ou intervalo relevante antes da retomada.

## Relatório na ordem solicitada

1. **SHA antes:** `79a082985a73c98e22fd31673c0805275ea93488`, branch main, repo `/Users/luizangelo/Documents/wacrm/wacrm`.
2. **SHA depois:** `07258bd2618462111dc78a3065152764ce64263f`. Dois commits locais autorizados: `7803643` hardening Auth e `07258bd` CLI. Sem push/force push. Diff anterior ao patch entre imagem-source `ed9ebd7` e HEAD inicial só envolve scripts/deploy/documentação/testes, nenhum novo código de aplicação. Alterações pré-existentes fora do escopo foram preservadas e NÃO incluídas nesses commits. Este relatório fica local, ainda não versionado.
3. **Imagem anterior/atual:** `wacrm-staging:ed9ebd7`; Image ID verificado `sha256:77245e16749c6671db1daada46894a0122f90f4bdbeb9355605a223e8a9863f0`.
4. **Imagem nova:** não construída; tag planejada `wacrm-staging:07258bd`. Build não executado, não foi declarada aprovação de imagem Docker com base no build Next local.
5. **Backup criado:** `/Users/luizangelo/Backups/wacrm/production/20260918T163807Z`, fora do repo/VPS/Supabase, diretório privado. Captura 16:38:08.454Z–16:39:19.049Z; banco custom `909870 bytes`, SHA-256 `c468d9f0a8997a8ae2c13688a228d0a261756e9cb3d48d8c69362f8102cce74b`. TOC e schema offline PASS. Manifesto SHA-256 revalidado independentemente PASS. Storage metadata/inventário e bytes copiados separadamente: 1 objeto, 392255 bytes, checksum preservado, inventários antes/depois iguais. Snapshot DB e captura Storage separados, sem freeze/atomicidade cross-service; este novo dump não foi restaurado nesta etapa. Backup contém material Auth/DB sensível e exige tratamento como segredo, embora nenhum secret operacional do Docker/libpq tenha sido exportado.
6. **Migration aplicada:** NÃO. Ledger continua com 48 entradas; `20260918161120` e nome `sync_auth_email_to_profile` ausentes. Última migration remota 21G `20260918042320`, não reaplicada.
7. **Função/trigger:** nova função ausente no pré-check produtivo. SQL revisada e testada em laboratório nativo PG17: função privada, trigger UPDATE email, filtro `profiles.user_id=auth.users.id`, search_path/ACL e invariância tenant/B PASS. Isso não substitui validação pós-aplicação: função/trigger/grants produtivos novos ainda NÃO homologados. RLS produtiva existente: 40/40 tabelas public com RLS. Nenhuma mutation real de e-mail realizada.
8. **Deploy:** NÃO EXECUTADO. Manifesto remoto, stack e serviços não alterados. Sem cópia de código/CLI para containers, sem migration, sem build na VPS, sem service update.
9. **App:** `wacrm_staging_app` 1/1, healthy, RestartCount=0, OOM=false; domínio/app Site URL corretos, NODE_ENV=production; `/login` HTTP 200.
10. **Scheduler:** `wacrm_staging_meta_conversions_scheduler` 1/1, healthy, RestartCount=0. Imagem e configuração existentes preservadas.
11. **Smoke Auth:** login page HTTP 200 e Auth public settings HTTP 200. Login/logout/signup reais, mudança de senha e navegação autenticada NÃO executados: nenhuma credencial/identidade controlada foi fornecida/confirmada nesta autorização. Não presumir que o único usuário produtivo seja fixture descartável. Comportamentos signup/password/forgot/change-email/nome/avatar/invitation/onboarding cobertos pelos testes locais, mas código novo ainda não está ativo em produção. Sem recovery/e-mail de teste.
12. **CLI segura:** 19/19 testes locais com mocks, inclusive SDK oficial com transporte simulado, senha sem eco, projeto/chave errados, zero retry, sanitização de logs e isolamento A/B. Inspect em produção NÃO executado nem ferramenta disponibilizada remotamente; requer artefato operacional privado e usuário controlado identificado. Não há novo endpoint/botão/API web. Dockerfile atual não empacota `scripts/` no runner; não assumir que npm command esteja disponível no container standalone sem distribuição operacional separada.
13. **Multi-tenancy:** nova execução PG17 local 139 verificações PASS; concorrência 3 cenários PASS; fingerprints do restore histórico intactos, clone de concorrência removido e laboratório parado. Sem app/scheduler/integrations ligados ao laboratório. Nova execução Vitest 125 arquivos/1484 testes PASS; Node equivalência/scheduler/checker 21 PASS; CLI 19 PASS; typecheck PASS; lint 0 erros/40 warnings legados. Membership produtiva continua em profiles (não existe account_members neste modelo); accounts=1, profiles=1, auth.users=1, auth.identities=1. Nenhuma alteração realizada nessas relações.
14. **WhatsApp:** config connected=1, contagem/vínculos produtivos não alterados. Inbound/outbound/status NÃO exercitados por novo envio nesta etapa; ausência de deploy preserva o estado anterior, sem alegar um novo teste ponta a ponta.
15. **Meta:** 8 eventos antes/depois do backup e na revalidação; pending=0, sending=0. Execuções automáticas observadas 16:30–16:38Z HTTP 200, intervalo 120000ms, processed/sent/failed/delivery_unknown/errors=0. Nenhum cron manual, POST /events ou alteração Meta executado pelo operador.
16. **Storage:** 3 buckets privados, 0 públicos, 1 objeto. Bytes autenticados HTTP 200, 392255 bytes, SHA-256 `f90cd9e4738db46573ba438abf4bedc460cea086aea08cf8ad473327aff9b267`. URL anônima legada e com nonce HTTP 400; checker runtime PASS. Nenhuma alteração em objeto/bucket, purge ou signed URL de teste. Autorização de assinatura continua coberta por testes locais existentes; não houve nova sessão autenticada para homologá-la visualmente.
17. **Logs:** app, últimos 30min: 0 bytes/0 linhas retornadas, sem padrão crítico nessa janela (ausência de logs não é prova de ausência de todos os erros históricos). Scanner dentro do container: zero matches de secrets, PII e ctwa_clid; zero Bearer/URI de senha/raw body. Scheduler em janela recente com contadores sanitizados/HTTP 200. Só fatos allowlisted saíram do processo.
18. **VPS:** Swarm active, Docker 28.1.1, amd64. Última coleta: RAM 3818MiB total/2878MiB usada/940MiB disponível; swap 2047MiB total/811MiB usada; disco 38G total/26G usado/11G livre, 72%; load 0.03/0.06/0.05. Não realizou load test, prune, resize nem alterações dos outros serviços.
19. **Warnings/gates:** caminho de build externo pendente; orçamento específico para build na VPS compartilhada não definido. 40 warnings legados preservados. Configuração SMTP própria ausente e password_min_length global=6 são contexto administrativo fornecido pelo usuário, NÃO uma nova leitura de Management API nesta etapa. Somente mailer_autoconfirm=true/disable_signup=false foram reconfirmados pelo endpoint público; nenhuma configuração administrativa alterada. Hardening WACRM mínimo 8 e modos suporte estão commitados, ainda não ativos. Cadastro público direto ao Auth continua sujeito à política global, não à validação interna WACRM.
20. **Rollback:** imagem anterior existe e foi verificada; serviço continua nela, portanto nenhum rollback necessário/executado. Após eventual deploy, voltar somente app para a tag anterior, preservando scheduler e migration aditiva saudável; registrar backup do manifesto antes do futuro update. Não restaurar banco inteiro, não reverter e-mails sincronizados, não alterar eventos Meta. Nenhum arquivo remoto de rollback foi criado porque o manifesto não foi modificado.

## Confirmações de escopo

- `crm.luizangelo.com.br` saudável no estado **anterior**, não confundir com rollout do novo Auth.
- Único Supabase consultado/capturado: `awganmhowivedfocwzjy`; Shodisparo `zyqbgrrpedzxfcwhlfoa` não foi tocado.
- Meta, DNS, SMTP/Resend/providers e Auth Management API/configuração externa não foram alterados.
- Confirmação e-mail continua como observada: mailer_autoconfirm=true. Nenhuma alteração da política global de senha; valor 6 mantido conforme contexto fornecido, sem afirmar auditoria administrativa nova.
- WACRM mínimo 8, forgot/change-email em suporte administrativo: **código novo local/commitado, implantação pendente**.
- CLI não está publicamente exposta; nenhum secret exibido, nenhum cliente real teve senha/e-mail alterado para homologação.
- .pgpass nunca foi lido/reproduzido por este operador/script; libpq usa o arquivo existente internamente. A captura não revelou passwords, keys ou tokens.
- Alterações deste turno: novos artefatos privados de backup, commits locais do escopo Auth/CLI e este relatório local. Nenhuma mutation produtiva.

## Próxima decisão antes de retomar

Disponibilizar ambiente Docker Linux/amd64 externo à VPS (preferido pelo runbook), ou autorizar especificamente um plano revisado de build na VPS compartilhada, com limites CPU/RAM/swap e janela/capacidade que proteja os serviços existentes. Não criar worker pago, instalar Docker/VM ou iniciar build pesado sem essa definição. Depois revalidar backup/estado/ledger e continuar somente a migration autorizada, build/deploy/smokes e CLI privada, parando em qualquer erro.
